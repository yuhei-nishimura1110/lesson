列挙型
public enum job(){
    TECHER(1)
    AXER(2)

    private int id;
    private job(int id){
    this.id=id;
    }
    public int getId(){
    return id;
    }
}
のように定義でき

job job = new job;
job.TEACHER.getId();
のようにじっこうできる
